{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'tmux (new-session|send-keys|list-sessions|kill-session|attach|capture-pane)'; then echo '{\"permissionDecision\": \"allow\"}'; else echo '{\"permissionDecision\": \"ask\"}'; fi"
          }
        ]
      }
    ]
  }
}
