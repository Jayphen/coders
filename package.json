{
  "name": "coders-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest",
    "test:plugin": "vitest --project plugin",
    "dev:tui": "pnpm --filter @jayphen/coders-tui dev",
    "build": "pnpm -r build",
    "plugin:test": "claude --plugin-dir packages/plugin"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
